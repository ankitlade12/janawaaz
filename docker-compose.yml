services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: janawaaz
      POSTGRES_PASSWORD: janawaaz
      POSTGRES_DB: janawaaz
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
